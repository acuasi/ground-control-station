describe("Buffer interface", function() {
  
  it('can be written to', function() {});
  it('has a "data" event that feeds new data to a callback', function() {});
  it('has a "error" event that feeds errors to a callback', function() {});
  it('has a "disconnected" event that triggers a callback', function() {});

});